% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etl.R
\name{ukb_worklife}
\alias{ukb_worklife}
\title{UKB 工作史 ETL}
\usage{
ukb_worklife(
  path,
  entry_path = NULL,
  covariate_path = NULL,
  sex = NULL,
  fields = NULL,
  age_min = DEFAULTS$age_min,
  age_max = DEFAULTS$age_max,
  fill_year = DEFAULTS$fill_year,
  verbose = TRUE
)
}
\arguments{
\item{path}{csv 路径，或一个已读入的 \code{data.frame} / \code{data.table}。}

\item{entry_path}{Category 123 的 csv 路径（含 \code{When_occupational_data_entered}）。
\strong{最容易漏的一个字段}：它在 Category 123 不在 130。缺它会回落到常数
\code{fill_year}，代价是 ongoing 记录平均多算约 2 年 = 面板的 5.7\%，
且退休观察率从 56.8\% 掉回 27.7\% —— 那就不是论文的口径了。}

\item{covariate_path}{协变量 csv 的路径（含 field 31 性别，可能也含 field 34
出生年）。\strong{真实的 Category 130 导出里通常没有性别列} —— 它是 field 31，
属于基线数据，要单独提供。}

\item{sex}{性别向量（与行对齐），或列名。缺省时依次在主表、\code{covariate_path}
里找 \code{Sex__0_0} / \code{p31} / \code{sex}。}

\item{fields}{覆盖默认列名映射的命名列表，如 \code{list(soc4 = "my_soc_col")}。}

\item{age_min, age_max}{年龄窗口。\code{age_max = 75} 是\strong{绑定约束}，
参考队列 6.7\% 的人被它封顶。}

\item{fill_year}{\code{entry_path} 缺失时 ongoing 的回落填充年。}

\item{verbose}{打印各步人数与关键口径的实测占比。}
}
\value{
\code{ukbcareer_worklife} 对象（一个列表）：\code{jobs} / \code{gaps} / \code{annual} /
\code{cohort} / \code{exposure}，外加 \code{attr(x, "ukbcareer")} 里的口径元数据。
}
\description{
把 UKB Category 130（+ Category 123 的 22500）的宽表变成五张干净的表：
job 事件表、gap 事件表、逐年面板、队列表（含 \code{career_end}）、暴露汇总。
这是本包 Tier 0 的全部产出，\strong{不需要 bundle、不需要模型权重}。
}
\examples{
# 用包内的合成假人演示（真实用法把 ukb_synth() 换成你的 csv 路径）
wl <- ukb_worklife(ukb_synth(n = 40), verbose = FALSE)
names(wl)
}
