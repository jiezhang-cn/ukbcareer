% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/camsis.R
\name{ukb_camsis}
\alias{ukb_camsis}
\title{附上 CAMSIS 职业地位}
\usage{
ukb_camsis(x, path, slope_flat = 0.05, verbose = TRUE)
}
\arguments{
\item{x}{\link{ukb_worklife} 的返回值。}

\item{path}{CAMSIS 表的 csv 路径，或一个 data.frame。需要三列：
\code{soc2000}（4 位码）、\code{mcamsis}、\code{fcamsis}。列名大小写不敏感。}

\item{slope_flat}{判"平"的斜率门槛（CAMSIS 点/年），默认 0.05。}

\item{verbose}{打印覆盖率与轨迹型构成。}
}
\value{
在 \code{x} 上加两项：\code{camsis}（逐人汇总）与 \code{camsis_by_age}（逐人年长表，
画年龄轨迹要用它，逐人汇总量给不了）。
}
\description{
\strong{CAMSIS 不进 Transformer 的输入}（它是 SOC 码的确定性函数，同码同分，
进输入等于把职业码换一种写法喂两遍）。它的价值恰在于\strong{独立于表征} ——
"latent space 是否恢复了职业地位梯度"只有在它没进输入时才是有意义的验证。
}
\details{
\strong{必须按性别选列}（\code{mcamsis} / \code{fcamsis}）：性别特异性已内建于量表。
后果是\strong{两性的纵轴不是同一把尺子} —— 可比"横向流动占多少"，
不可比"某个高度对应什么职业"。

只在 \code{career_end} 之前的\strong{在职}人年上计算（与 token 同窗口）。
}
\examples{
\dontrun{
wl <- ukb_worklife("ukb_cat130.csv", entry_path = "ukb_cat123.csv")
wl <- ukb_camsis(wl, "camsis_soc2000.csv")
}
}
