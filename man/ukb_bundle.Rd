% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bundle.R
\name{ukb_bundle}
\alias{ukb_bundle}
\title{载入 bundle}
\usage{
ukb_bundle(path, verify = TRUE, quiet = FALSE)
}
\arguments{
\item{path}{A local bundle directory. This is the only supported route; the
bundle is distributed under controlled access.}

\item{verify}{Verify the sha256 of every file. Default \code{TRUE}.
\strong{Do not turn this off} -- it is where the "frozen representation"
discipline lands on the R side.}

\item{quiet}{Suppress the summary.}
}
\value{
A \code{ukbcareer_bundle} object.
}
\description{
载入 bundle
}
\details{
To obtain a bundle see \code{BUNDLE-ACCESS.md} in the package sources, or
\url{https://github.com/jiezhang-cn/ukbcareer/blob/main/BUNDLE-ACCESS.md}.
\strong{There is no automatic download}: the weights derive from UK Biobank data,
and distribution requires the recipient to hold their own application --
a step code cannot take on your behalf.

\strong{The path must be pure ASCII.} libtorch resolves paths through the system
ANSI encoding, so non-ASCII characters (a non-Latin user name, for instance)
make \code{jit_load()} report "Parent directory does not exist". This function
copies the encoder to an ASCII cache directory and says so; choose the
location with \code{options(ukbcareer.ascii_cache = "...")}.
}
\examples{
\dontrun{
b <- ukb_bundle("~/ukbcareer_bundle_v5.0")
b
}
}
