% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-report.R
\name{ukb_report}
\alias{ukb_report}
\title{Write whole-career reports for several people}
\usage{
ukb_report(x, eid, file, width = 9, height = 8.5, ...)
}
\arguments{
\item{x}{The result of \code{\link[=ukb_worklife]{ukb_worklife()}} or \code{\link[=ukb_career]{ukb_career()}}. Results of
\code{\link[=ukb_camsis]{ukb_camsis()}}, \code{\link[=ukb_movement]{ukb_movement()}} and \code{\link[=ukb_typology]{ukb_typology()}} are picked up and
shown in the summary when present.}

\item{eid}{One or more eids. Use \code{sample(wl$cohort$eid, 20)} for a random
check.}

\item{file}{Output path, \code{.pdf} (any number of people) or \code{.png} (one
person).}

\item{width, height}{Page size in inches.}

\item{...}{Passed on to \code{\link[=plot_person]{plot_person()}}.}
}
\value{
The file path, invisibly.
}
\description{
Calls \code{\link[=plot_person]{plot_person()}} for each \code{eid} and writes the pages to one
multi-page PDF (or, for a single person, to a PNG if \code{file} ends in
\code{.png}). Handy for eyeballing a random sample before analysis.
}
\examples{
\dontrun{
wl <- ukb_worklife(ukb_synth(n = 60), verbose = FALSE)
ukb_report(wl, eid = wl$cohort$eid[1:5], file = "career_reports.pdf")
}
}
