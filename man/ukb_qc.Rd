% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{ukb_qc}
\alias{ukb_qc}
\title{Quality control: compare your definitions against the reference cohort}
\usage{
ukb_qc(x, bundle = NULL, quiet = FALSE)
}
\arguments{
\item{x}{The result of \link{ukb_worklife} or \link{ukb_career}.}

\item{bundle}{The result of \link{ukb_bundle}, which supplies the reference
distributions. With \code{NULL} only internal consistency is checked.}

\item{quiet}{Return the table without printing.}
}
\value{
A \code{data.table} with \code{check} / \code{value} / \code{value_num} / \code{reference} /
\code{kind} / \code{status} / \code{note}. \code{status} is one of \code{ok} / \code{warn} / \code{fail} /
\code{info}; \code{kind} is \code{cohort} (compared against the reference cohort, where
a deviation may simply be a real sample difference) or \code{internal}
(compared against a value that must hold, where a deviation is a bug).
}
\description{
Every check points at one specific cause:
}
\details{
\tabular{ll}{
   Check \tab What exceeding it means \cr
   SOC out-of-vocabulary rate \tab your SOC coding version differs from the frozen vocabulary (expected near 0) \cr
   \code{career_end_source} shares \tab field 22500 is missing, or an index date was used as the cap \cr
   median \code{seq_len} \tab a systematic problem in the ETL \cr
   share hitting \code{max_len} \tab the reference cohort's maximum is exactly 60, so hitting 64 means the definitions differ \cr
   share hitting \code{age_max} \tab a different age definition \cr
   "do not know" rate \tab \strong{the most common error}: negative exposure codes were cleaned away as missing \cr
   gap person-year share \tab the panel was not built from job UNION gap (which loses 84.6\% of gap person-years) \cr
   parallel / transition shares \tab the two kinds of "two jobs in one year" were not separated \cr
}
}
\examples{
wl <- ukb_worklife(ukb_synth(n = 60), verbose = FALSE)
ukb_qc(wl)
}
