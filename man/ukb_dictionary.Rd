% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dictionary.R
\name{ukb_dictionary}
\alias{ukb_dictionary}
\title{What each derived variable means, and how to use it}
\usage{
ukb_dictionary(variable = NULL, what = NULL, expand = FALSE)
}
\arguments{
\item{variable}{Optional variable name(s) to look up.}

\item{what}{Optional variable group(s), as in the \code{what} argument of
\code{\link[=ukb_career]{ukb_career()}}: \code{"core"}, \code{"exposure"}, \code{"camsis"}, \code{"movement"},
\code{"type"}, \code{"latent"}.}

\item{expand}{If \code{TRUE}, write out the four per-agent rows for each of the
ten agents (40 rows) instead of one \verb{<agent>} row each.}
}
\value{
A \code{data.table} with columns \code{variable}, \code{what}, \code{group}, \code{needs},
\code{description} and \code{interpretation}. Printing it shows a readable summary.
}
\description{
A dictionary of every column that \code{\link[=ukb_career]{ukb_career()}} can return. For each
variable it gives a short description, what it needs, and advice on how to
interpret and use it.
}
\details{
Look up one variable with \code{ukb_dictionary("career_end")}. Agent-specific
names such as \code{"yrs_high_asbestos"} are also recognised. List a whole group
with \code{ukb_dictionary(what = "exposure")}.

The ten workplace exposures (\verb{<agent>}) are \code{noisy}, \code{cold}, \code{hot},
\code{dusty}, \code{fumes} (chemical or other fumes), \code{cigarette} (other people's
smoke), \code{asbestos}, \code{paints} (paints, thinners or glues), \code{pesticides} and
\code{diesel} (diesel exhaust), each answered per job as rarely/never, sometimes
or often (UK Biobank fields 22606-22615).
}
\examples{
ukb_dictionary()
ukb_dictionary("career_end_source")
ukb_dictionary(what = "exposure")
# as a plain table, e.g. to export for a data-analysis plan
as.data.frame(ukb_dictionary(expand = TRUE))[1:3, 1:4]
}
