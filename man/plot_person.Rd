% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-report.R
\name{plot_person}
\alias{plot_person}
\title{Whole-career report for one person}
\usage{
plot_person(x, eid, age_range = NULL, show_tokens = TRUE, base_size = 8)
}
\arguments{
\item{x}{The result of \code{\link[=ukb_worklife]{ukb_worklife()}} or \code{\link[=ukb_career]{ukb_career()}}. Results of
\code{\link[=ukb_camsis]{ukb_camsis()}}, \code{\link[=ukb_movement]{ukb_movement()}} and \code{\link[=ukb_typology]{ukb_typology()}} are picked up and
shown in the summary when present.}

\item{eid}{The person to draw.}

\item{age_range}{Ages to show, \code{c(from, to)}. Default: 16 to 65, extended
to the career end when it is later.}

\item{show_tokens}{Draw the model-specific markers (\verb{[CLS_W]}, \code{PAD}).
Set \code{FALSE} for a plainer figure.}

\item{base_size}{Base font size in points.}
}
\value{
A patchwork object (print it, or save it with \code{\link[=ukb_save]{ukb_save()}}; a good
size is \verb{width = 9, height = 8.5}). Needs ggplot2 and patchwork.
}
\description{
Draws one person's entire work history on a single page, one column per
year of age, in the layout of Figure 1a of the accompanying paper. It is the
quickest way to check what the package made of a person's raw UK Biobank
records -- and to show collaborators what the derived variables mean.
}
\details{
From top to bottom:
\itemize{
\item \strong{Summary} (title block): the person-level derived variables -- when the
career ended and why, working years, number of occupations and changes,
exposure breadth, shift and long-hours share, plus CAMSIS, mobility and
career type when those have been computed.
\item \strong{state}: the annual state (full-time, part-time, unemployed, home and
family, education, marginal work, health, retired, other, unknown).
\item \strong{SOC 4-digit to 1-digit}: the occupation code at the four levels of the
SOC2000 hierarchy. Consecutive years in the same code merge into one
labelled block, so a move within the same minor group (say 4123 to 4122)
shows as two blocks on the 4-digit row but one block on the rows above.
\item \strong{gap code}: the UK Biobank gap code (103 education, 105 home/family,
106 illness or disability, 108 retirement, ...) in years not in work.
\item \strong{age}: the age axis the model uses (one learned position per year).
\item \strong{dt}: tenure in the current occupation code, \code{min(tenure, 3) / 3}. A
break does not reset it (returning to the same job continues the spell); a
new code does.
\item \strong{jobs}: number of jobs held that year, with markers for a
\emph{transition} year (one job ends as another starts) and a \emph{parallel} year
(two jobs held at the same time).
\item \strong{exposure intensity}: the ten workplace exposures (never / sometimes /
often), weekly hours / 48, shift level / 2 (none / shifts / including
nights) and the share of exposures answered "do not know" (a \verb{?} marks
which ones). Every row is drawn on its own 0-to-maximum scale, so the
darkest colour means "often", 48+ hours or night shifts respectively.
}

Hatched cells are years not in work, where occupation and exposures are
\emph{undefined} rather than zero. The red dashed line is \code{career_end}; the grey
area after it (\code{PAD}) is not used by the model or by any summary. Blank
columns are years with no job or gap record at all.
}
\examples{
d  <- ukb_synth(n = 60)
wl <- ukb_worklife(d, verbose = FALSE)
if (requireNamespace("patchwork", quietly = TRUE)) {
  p <- plot_person(wl, eid = attr(d, "showcase_eid"))
  # ukb_save(p, "person.pdf", width = 9, height = 8.5)
}
}
\seealso{
\code{\link[=ukb_report]{ukb_report()}} to write reports for many people to one PDF;
\code{\link[=ukb_dictionary]{ukb_dictionary()}} for the definitions of the summary variables.
}
